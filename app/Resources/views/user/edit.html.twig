{% extends 'base.html.twig' %}

{% block body %}

    EDIT USER TEMPLATE<br>

    {{ user.name }}, {{ user.surname }}

    {{ form_start(form) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}

    isSubmitted: {{ isSubmitted ? 'true' : 'false' }}
    isValid: {{ isValid ? 'true' : 'false' }}

{% endblock %}